# Lines tolerated by `scripts/coverage_gate.sh` despite reporting zero
# execution count. Each entry is `<rel-path>:<line>` followed by a
# `# reason`. Reviewers should push back on any new addition without a
# concrete justification.
#
# Convention adopted from `ippoan/cc-relay` — opt-in 100% enforcement
# on files listed in `coverage_100.toml`, with per-line carve-outs
# documented inline here.
#
# Categories:
#
# - **test-assertion-panic**: `match { ... _ => panic!("expected X") }`
#   pattern. fires only if the assertion fails — unreachable on green CI.
#
# - **defensive-body-error**: `axum::Body` collect error path. reqwest/axum
#   normal Body 実装 (Full / BoxBody from rmcp StreamableHttpService) は
#   collect() で error を返さない。custom Body を作るためだけに 1 行のために
#   mod 全体を refactor する価値が無いので allowlist。

# --- test-assertion-panic ---
# Paths shifted to `crates/mcp-relay/src/...` in issue #9 Phase 1 monorepo化.
# Line numbers shift again with issue #178 (Ping/Pong frame + keepalive_supported)。
crates/mcp-relay/src/relay/frame.rs:238  # `_ => panic!("expected Hello")` 残余 arm (旧 198)
crates/mcp-relay/src/relay/frame.rs:277  # `_ => panic!("expected Pong")` 残余 arm (issue #178)
crates/mcp-relay/src/relay/frame.rs:331  # `else { panic!("expected Req") }` (旧 252)

# --- defensive-body-error ---
crates/mcp-relay/src/relay/bridge.rs:69  # `Err(e) => error_resp(500, "response collect failed: {e}")`
